version: "3.9"

services:
  app:
    build: .
    image: p12-app:local
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      - APP_ENV=production
    security_opt:
      - no-new-privileges:true
    read_only: true
